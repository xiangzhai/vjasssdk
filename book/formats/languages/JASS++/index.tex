\documentstyle[german]{report}
\begin{document}
	\title{JASS++}
	\author{Tamino Dauth \and \verb+tamino@cdauth.eu+\thanks}
	\date{2010-07-07}
	\maketitle
	\begin{abstract}
		Dieses Dokument enthält die Definition der Skriptsprache JASS++.
	\end{abstract}
	\tableofcontents
	\section{\dots}

	Ein minimales \LaTeX{}--Dokument.

	1. Vorwort
	2. Laufzeit und Kompilierung
	3. Kompatibilität zu anderen Sprachen
	4. Verfügbarkeiten
	5. Schlüsselwörter
	6. Kommentare
	7. Präprozessoren
	8. Testmodus (debug mode)
	9. Deklarationen
	12. Bezeichner (identifiers)
	10. Deklaration nativer Typen
	11. Deklaration nativer Funktionen
	13. Literale
	14. Zugriffsmodifikatoren
	15. Freunde
	16. Gültigkeitsbereiche (scopes)
	17. Gültigkeitsbereichverwendung
	18. Qualifizierer-Gültigkeitsbereiche
	19. Testblöcke
	20. Anweisungsblöcke
	21. Variablen
	22. Operatoren
	23. Typen
	24. Verzweigungen
	25. Schleifen
	26. Variablensperrung (locking)
	27. Funktionen
	28. Funktionsvariablen (Funktionszeiger)
	29. Funktionstypen
	30. Lambda-Funktionen
	31. Funktionsaufrufe
	32. Ausnahmebehandlung
	33. Finally
	34. Pseudonyme (aliases)
	35. Aufzählungen (enums)
	36. Vorlagen (templates)
	37. Klassen
	38. Elemente
	39. Methoden
	40. Operatorenüberladung
	41. Serialisierung
	42. Lade-Blöcke
	43. KI-Blöcke
	44. Synchronisationsblöcke
	45. Die JASS-Bibliothek
	46. Die Standardbibliothek

	\include{preface/index}
	\include{runtime and compilation/index}
	\include{compatiblity to other languages/index}
	\include{availibilities/index}
	\include{keywords/index}
	\include{comments/index}

	\include{preprocessors/index}
	\include{debug mode/index}
	\include{debug qualifiers/index}

	\include{identifiers/index}
	\include{literals/index}
	\include{access modifiers/index}
	\include{friends/index}
	\include{scopes/index}
	\include{scope usage/index}
	\include{qualifiers scopes/index}

	\include{aliases/index}

	\include{types/index}

	\include{declarations/index}
	\include{declarations of native types/index}
	\include{declarations of native functions/index}

	\include{variables/index}
	\include{operators/index}

	\include{statement blocks/index}
	\include{intersections/index}
	\include{loops/index}
	\include{exception handling/index}
	\include{finally/index}
	\include{variables locking/index}
	\include{synchronization blocks/index}

	\include{functions/index}
	\include{function types/index}
	\include{lambda functions/index}
	\include{operators overloading/index}
	\include{function calls/index}

	\include{enums/index}

	\include{templates/index}

	\include{classes/index}

	\include{serialization/index}
	\include{load blocks/index}
	\include{ais/index}

	\include{jass library/index}
	\include{standard library/index}
\end{document} 
